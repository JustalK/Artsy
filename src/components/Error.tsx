import React from 'react';
import { TextLine } from 'src/styles/Main';

/**
 * Manage the error screen of the application
 * @return {JSX.Element} The error display
 **/
const Error = (): JSX.Element => {
	return <TextLine>Sorry, I did not take that into account.</TextLine>;
};

export default Error;
